services:
  app:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - .:/app
      - /app/node_modules
      - wrangler-state:/app/.wrangler
    command: npm run dev:docker

volumes:
  wrangler-state:
